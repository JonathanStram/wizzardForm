import React from 'react';
import axios from 'axios';

class AjaxPostRequest extends React.Component {
    state = {
        //this props shauld be here
    }

    saveInpust = (e) => {
        let key  = e.target.name
        
    }
}

export default AjaxPostRequest;